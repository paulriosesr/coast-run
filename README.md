# BLOOD NEON: LAST WATCH

Phaser 3 + TypeScript vertical slice — **Level 1: Haven’s Edge** only.

Shelved project: COAST RUN → `archive/coast-run/`.

## Run locally

```bash
cd app
npm install
npm run dev
```

Open **http://localhost:5173/**

Flow: Title → hunter select (Rex / Nova / Silas / Vesper) → L1 alley → waves → Pale Broker → clear tease (LEVEL 2 LOCKED).

Proto rects until Picasso audits real sheets into `app/public/assets/`.

## Docs

- `GDD.md`, `controls.md`, `palette.md`, `asset-list.md`, `playtest.md`
